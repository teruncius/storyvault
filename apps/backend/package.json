{
    "name": "@storyvault/backend",
    "scripts": {
        "dev": "cargo watch --ignore .turbo --ignore --ignore tests --ignore data/tmp -x run",
        "build": "cargo build --release",
        "format": "cargo fmt",
        "lint": "cargo clippy"
    },
    "dependencies": {
        "react-router": "^7.9.6"
    }
}