{
    "name": "@storyvault/backend",
    "scripts": {
        "build": "cargo build --release",
        "dev": "cargo watch --ignore .turbo --ignore --ignore tests -x run",
        "format": "cargo fmt",
        "lint": "cargo clippy"
    }
}
